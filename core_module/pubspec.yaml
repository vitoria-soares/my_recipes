name: core_module
description: Core module - shared services, errors and third-party package exports.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^9.2.1
  flutter_bloc: ^9.1.1
  flutter_modular: ^6.4.1
  dio: ^5.9.2
  google_fonts: ^8.1.0
  dartz: ^0.10.1
  equatable: ^2.0.8
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

flutter:
